# Third-party notices

The release bundle has no separately bundled runtime library.

The plugin calls the PDF.js build supplied by Obsidian through Obsidian's public `loadPdfJs()` API. PDF.js is provided by the host application and is not redistributed by this repository.

Development tools are listed in `package.json` and are not included in the runtime release.
